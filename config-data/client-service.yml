server:
  port: 8092

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8099/eureka/

Feing:
    hystrix:
        enabled: true
management:
    endpoints:
        web:
            exposure:
                include: "*"
