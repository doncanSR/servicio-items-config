server:
  port: 8092

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8099/eureka/

Feing:
    hystrix:
        enabled: true
#hystrix:
#    dashboard:
#        proxy-stream-allow-list: '*'
management:
    endpoints:
        web:
            exposure:
                include: '*'
